<?php

return [
    /*
     * Цепочка маршрутов в порядке приоритета; `null` — прямое соединение.
     * Соблюдается буквально: что не перечислено — не используется. Смена
     * маршрута происходит только когда запрос не ушёл в сеть (мёртвый маршрут,
     * DNS, отказ в соединении, порванный CONNECT-туннель) — read-таймаут и
     * любой HTTP-ответ маршрут не меняют.
     */
    'proxies' => [
        null,                              // сначала напрямую
        // 'http://1.1.1.1:12345',         // затем прокси 1
        // 'http://2.2.2.2:12345',         // затем прокси 2
    ],
    'verify' => true,                     // Проверка TLS сертификата для HTTPS запросов
    'timeout' => 60,                       // Maximum number of seconds to wait for a response
    'connectTimeout' => 10,                // Maximum number of seconds to wait while trying to connect to a server
    'retries' => 2,                        // количество попыток.  Если у вас 3 прокси, то при 'retries' => 2, будет совершено 6 запросов (2 * (1 + 2))
    'retryDelay' => 1000,                  // Задержка перед повтором в миллисекундах
];
